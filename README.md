# carver

C tool that identifes embedded files by scanning for known file signatures. Incomplete - buggy and incomplete logic flow, work in progress

### Build
```bash
# Using gcc
gcc carver.c -o carver -g -Wall -Wextra -pthread

```

